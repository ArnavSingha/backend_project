[build]
  command = "npm run build"
  publish = "dist"

# Redirects for SPA - handle client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables (set these in Netlify dashboard)
# VITE_API_URL = https://your-backend-url.onrender.com/api
